import { SecondaryButtonComponent } from './secondary-button.component';

describe('SecondaryButtonComponent', () => {
  let component: SecondaryButtonComponent;

  beforeEach(async () => {
    component = new SecondaryButtonComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
